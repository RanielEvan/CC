#include <stdio.h>

int main() {
	
	double pi = 3.14159;
	double raio;
	double area = pi * (raio * raio);
	float total
	
	
	scanf("%f %f", &qtd, &valor);
	total = valor * qtd;
	
	printf("%.2f \nO valor digitado foi %.2f e a quantidade foi %d", total, valor, qtd);
	
	
	return 0;
	
}
